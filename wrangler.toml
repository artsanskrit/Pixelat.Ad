# Wrangler configuration for Cloudflare Workers with static assets
name = "pixelatad"
compatibility_date = "2024-09-23"

# Serve the Next.js static export as a Workers Assets site (no main script needed)
[assets]
directory = "./out"
